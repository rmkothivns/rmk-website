{
  "name": "rmk-website",
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "workspaces": [
    "src/frontend"
  ],
  "scripts": {
    "start": "cd src/frontend && npm install && npx vite --host",
    "dev": "cd src/frontend && npm install && npx vite --host",
    "build": "npm run build --workspaces --if-present && node -e \"const fs = require('fs'); fs.cpSync('src/frontend/dist', 'dist', { recursive: true });\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "fix": "npm run fix --workspaces --if-present"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.10.0",
    "sharp": "^0.34.4"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
